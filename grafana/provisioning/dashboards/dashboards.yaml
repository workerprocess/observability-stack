apiVersion: 1

providers:
  - name: Core Telemetry Dashboards
    orgId: 1
    folder: Core Telemetry
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
